import { FullNameValidDirective } from './full-name-valid.directive';

describe('FullNameValidDirective', () => {
  it('should create an instance', () => {
    const directive = new FullNameValidDirective();
    expect(directive).toBeTruthy();
  });
});
