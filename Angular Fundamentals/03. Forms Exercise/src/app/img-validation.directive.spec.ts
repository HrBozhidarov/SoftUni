import { ImgValidationDirective } from './img-validation.directive';

describe('ImgValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgValidationDirective();
    expect(directive).toBeTruthy();
  });
});
